# PM7-WEB-PRODUCTIVO

Build estático compilado de [PM7_Web](https://github.com/JenniArabel/PM7_Web) (React/Vite, prerenderizado). No editar directamente acá — los cambios van en el repo de origen; este repo solo recibe el resultado de `npm run build` (carpeta `dist/`) para que Webempresa lo clone directo a producción.
